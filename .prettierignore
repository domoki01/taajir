.next/
node_modules/
package-lock.json

# Generated by scripts/build-geo.mjs. communes.json is intentionally minified —
# it is 1541 records that no one reads by hand.
src/data/geo/

# The Laravel port. It follows Laravel's own conventions — Pint for PHP, and
# four-space JS/CSS to match the framework's skeleton — so reformatting it to
# the Next app's Prettier config would only produce churn. Its checks are
# `vendor/bin/pint` and `php artisan test`, run from inside `laravel/`.
laravel/
